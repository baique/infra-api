pipeline {
    agent {
        docker {
            image 'docker-proxy.hljzj.tech/maven:3-alpine'
            args '-v /home/.m2:/root/.m2'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -DskipTests clean package'
            }
        }
    }
}